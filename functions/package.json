{
  "name": "pyeonghaeng-toktok-functions",
  "private": true,
  "main": "lib/index.js",
  "type": "module",
  "engines": { "node": "22" },
  "scripts": {
    "generate:prompts": "node tools/generate-prompts.mjs",
    "prebuild": "node tools/generate-prompts.mjs",
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0",
    "openai": "^4.77.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
